console.log('The gmail_tokens collection is no longer used.');
console.log('ThinkDesk Gmail sync now relies on Appwrite Google identities plus the gmail-api function.');
console.log('Nothing needs to be provisioned here.');
